# This file will apply the Kubernetes manifests after the cluster is ready
# We'll use kubectl apply instead for simplicity

# Note: The actual microservices will be deployed using kubectl
# after the cluster is provisioned
